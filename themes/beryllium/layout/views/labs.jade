extends ../base

block site_body
    .col.col-12.p2
        h1= page.title

        div!= page.content

        - var lab_items = site.categories.findOne({name: 'labs'}).posts.toArray()
        each post in lab_items
            a(href=url_for(post.path)).border-box.border.rounded.p2.mb2.block
                h1.h2= post.title


